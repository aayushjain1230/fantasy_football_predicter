# Optional provider keys for local testing or Streamlit Community Cloud.
# Leave values blank when unavailable. Do not put ESPN_S2 or ESPN_SWID in
# a shared public Streamlit deployment. Private-league users enter them in
# password-masked, session-only fields inside the app.

ODDS_API_KEY = ""
ENABLE_MARKET_ADJUSTMENTS = false
OPENWEATHER_API_KEY = ""
DIGEST_WEBHOOK_URL = ""
