import type { Metadata } from "next";import { Shell } from "../../components/Shell";import { PrivacyPage } from "../../components/PrivacyPage";export const dynamic="force-dynamic";export const metadata:Metadata={title:"Privacy Policy",description:"How Fourth Down stores fantasy league data, protects ESPN credentials, uses external providers, and lets users delete local data.",alternates:{canonical:"/privacy"},robots:{index:true,follow:true}};export default function Page(){return <Shell><PrivacyPage/></Shell>}
