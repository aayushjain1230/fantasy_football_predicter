"use client";
import { FormEvent, useState } from "react";
import { Database, ExternalLink, LockKeyhole, ShieldCheck, Trash2 } from "lucide-react";
import { api } from "../lib/api";
export function PrivacyPage(){const[confirmation,setConfirmation]=useState("");const[message,setMessage]=useState("");async function erase(e:FormEvent){e.preventDefault();if(confirmation!=="DELETE MY DATA")return;await api("/api/privacy/data",{method:"DELETE",body:JSON.stringify({confirmation})});setMessage("Stored league, cache, draft, and prediction data were deleted. The app is back in demo mode.");setConfirmation("")}return <main className="page"><header className="page-head"><span>PRIVACY & SECURITY</span><h1>Your league stays yours.</h1><p>Plain-language details about what Fourth Down stores, sends, and never collects.</p></header><div className="privacy-grid"><article><LockKeyhole/><h2>Secrets stay server-side</h2><p>ESPN cookies, odds keys, and webhook URLs are read from the backend environment. They are never returned by API responses or placed in browser storage.</p></article><article><Database/><h2>Local storage</h2><p>League snapshots, provider caches, draft state, and prediction history are stored in a local SQLite file. There is no hosted user database in the default setup.</p></article><article><ExternalLink/><h2>External requests</h2><p>Data goes to ESPN when connecting, The Odds API and Open-Meteo when refreshing context, nflverse when refreshing open data, and Discord only when you explicitly send a digest.</p></article><article><ShieldCheck/><h2>No tracking or ads</h2><p>The application contains no analytics tracker, advertising SDK, fingerprinting, paid LLM, or sale of personal information.</p></article></div><section className="policy"><h2>Privacy policy</h2><p>Fourth Down processes fantasy-league information to generate recommendations. The default local installation does not create user accounts or send stored league data to the developer. Provider requests necessarily disclose your server IP address and the minimum identifiers required by that provider. Cached data remains on your computer until you delete it. ESPN authentication cookies grant access to your league and should be treated like passwords.</p><p>This project is an independent fantasy analysis tool and is not affiliated with ESPN, the NFL, or sportsbooks. Recommendations are probabilistic entertainment and should not be used as gambling or financial advice.</p><small>Policy version: July 18, 2026. This policy describes the local application as shipped; a future hosted deployment would require a separate policy.</small></section><form className="danger-zone" onSubmit={erase}><Trash2/><div><h2>Delete local application data</h2><p>Type <b>DELETE MY DATA</b> to erase league snapshots, caches, draft state, and prediction history. Environment secrets are separate files and are not modified.</p><input value={confirmation} onChange={e=>setConfirmation(e.target.value)} placeholder="DELETE MY DATA"/><button disabled={confirmation!=="DELETE MY DATA"}>Permanently delete stored data</button>{message&&<p className="success-message">{message}</p>}</div></form></main>}
